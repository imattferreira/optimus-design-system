import type { StylesOf } from '~/@types/helpers';
import type { Sizes } from '../helpers/types';

export const SIZES: StylesOf<Sizes> = {
  size1: 'text-base',
  size2: 'text-sm',
  size3: 'text-xs'
};
